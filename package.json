{
  "private": true,
  "scripts": {
    "deps:install": "for d in ./*/ ; do (cd \"$d\" && npm install); done",
    "start": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop ecosystem.config.js"
  },
  "dependencies": {
    "pm2": "^5.3.0",
    "rimraf": "^5.0.1"
  },
  "packageManager": "yarn@3.6.1",
  "workspaces": [
    "packages/*"
  ]
}
